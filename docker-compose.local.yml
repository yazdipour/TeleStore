services:
  app:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - path: .env
        required: false
    volumes:
      - telegram-session:/data
    restart: unless-stopped

volumes:
  telegram-session:
